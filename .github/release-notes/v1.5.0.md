[English](#english) · [简体中文](#简体中文)

<a id="english"></a>
### English

> This release makes rapid SFTP work more reliable, restores missing parts of complex SVG diagrams, and smooths the editor, notifications, activity bar, and responsive layouts.

#### Important fixes

- Fixed SFTP files opened by double-clicking appearing against the lower-right edge of the window. The built-in editor now opens centered in the available Terma window while remaining draggable and keeping its existing fullscreen behavior.
- Fixed completion notifications for downloads, extraction, and other background tasks arriving several seconds late. Foreground notification checks now run about once per second while hidden windows keep a slower, battery-friendly interval.
- Fixed page notifications disappearing while they were being read. Hovering a notification or focusing one of its controls now pauses automatic dismissal and resumes the remaining time after the interaction ends.
- Fixed the activity bar drawing its blue selection line before the matching button and workspace were ready. The selected background and indicator now appear together after the current workspace render completes.
- Switching to Batch Commands, Import/Export, or Settings now reuses the rendered view immediately. Settings data refreshes in parallel in the background, import pages keep their existing DOM, and command templates avoid an unnecessary reload.
- Fixed rapid SFTP tab switching and repeated SFTP opens leaving later tabs stuck on the loading view. Each tab now keeps its own directory request and detached view, identical directory requests are shared without cancelling individual tabs, completed detached tabs repaint when restored, loading placeholders never enter the shared cache, and shared SSH channel errors no longer force-reconnect healthy sessions.
- Fixed SVG previews repeatedly shrinking during startup, becoming incomplete, or making large diagram files feel frozen. Preview fitting is now stable, valid producer-provided `viewBox` canvases stay authoritative, and large symbol-heavy SVGs avoid an unreliable full bounds scan.
- Fixed SVG search treating device relationship metadata as many separate results and zooming so tightly that nearby diagram content looked missing. Exact device IDs now resolve to the device itself, search keeps useful surrounding context, and the active result is outlined with its element name and dimensions; clearing the search restores the full fitted drawing.
- Fixed SFTP columns overlapping after a divider was dragged nearly closed. Narrow columns now keep a safe minimum width and hide content cleanly instead of painting through the next column.
- Fixed the archive destination folder chooser looking centered and uneven. Directory entries now use a consistent left-aligned tree-like row with fixed icon and arrow columns.
- Fixed the built-in editor layout becoming stacked and unbalanced after resizing the window. Its title, encoding/line-ending/language controls, history options, editor area, and bottom actions now reflow together based on the editor width.
- Unified select controls so encoding, line-ending, language, and other dropdowns use the same divider and one stable chevron instead of a mix of merged and doubled arrows.
- Fixed opening the same SFTP file repeatedly creating multiple editor windows. Terma now reuses the editor for the same connection and remote path, and restores it when it was minimized.
- Fixed SFTP directory reads getting stuck after switching to another tab and minimizing Terma. Background requests now finish for their own tab and repaint when that tab is shown again.
- Fixed completed SFTP uploads, downloads, deletes, copies, and drag-out jobs leaving their old running floating task card visible. Completion and failure notifications now settle the matching task immediately while the final server state is refreshed in the background.
- Fixed complex SVG diagrams losing class-based connection lines and parts of symbol-based controls in the preview. Sanitized embedded SVG styles are now applied in the preview's isolated rendering scope, preserving source strokes, fills, and visible symbol overflow.
- Fixed the SVG preview window growing or shrinking when the drawing was zoomed. The outer preview now keeps its own stable size while zooming only changes the scrollable drawing area inside it.

#### Changes in this release

- User-visible work in this release was committed directly by the project maintainer and is not associated with a merged PR or Issue.

**Full Changelog**: [v1.4.9...v1.5.0](https://github.com/zmide/Terma/compare/v1.4.9...v1.5.0)

<a id="简体中文"></a>
### 简体中文

> 本次更新提升快速 SFTP 操作的可靠性，恢复复杂 SVG 图纸中缺失的控件和线段，并改善编辑器、通知、活动栏和窄窗口布局体验。

#### 重要修复

- 修复双击打开 SFTP 文件时，内置编辑器贴近窗口右下角显示的问题。编辑器现在会在 Terma 当前可用窗口区域内居中打开，同时继续支持拖动和原有全屏行为。
- 修复下载、解压等后台任务完成后，提示要延迟几秒才出现的问题。Terma 前台现在约每秒检查一次完成事件，隐藏窗口仍使用较慢的省电间隔。
- 修复正在阅读的页面通知仍会自动消失的问题。鼠标悬停通知或聚焦通知内控件时，现在会暂停自动关闭，交互结束后再继续剩余时间。
- 修复活动栏先单独绘制蓝色选中竖线，等待工作区出现后才补全按钮的问题。现在会在当前工作区完成同一轮渲染后，一次显示完整的选中背景和指示线。
- 切换到“批量命令”“导入导出”或“设置”时，现在会立即复用已经渲染的界面。设置数据在后台并行刷新，导入页保留原有 DOM，命令模板也不会无意义地重复读取。
- 修复快速切换 SFTP 标签或连续打开多个 SFTP 时，后面的标签可能一直停在“正在读取目录”的问题。每个标签现在保留自己的目录请求和分离视图，相同目录请求会共享但不会取消单个标签，后台完成的标签返回时会重新绘制，加载中的占位状态不会写入共享缓存，健康的共享 SSH 会话也不会再因单个子通道错误被强制重连。
- 修复 SVG 预览打开后不断缩小、内容显示不完整以及大型图纸打开卡顿的问题。现在保留文件提供的有效 `viewBox` 完整画布，大量 symbol 的大型 SVG 也不会再使用不可靠的完整边界扫描。
- 修复 SVG 搜索把设备关系元数据算成大量独立结果，并因定位放大过度导致周边图纸看似缺失的问题。精确设备 ID 现在只定位设备本体，搜索会保留有用的周边范围，并用边框、元素名称和尺寸标签标出当前结果；清空搜索后自动恢复完整适应窗口。
- 修复拖动列分隔线把 SFTP 列压得过窄后文字重叠的问题。窄列现在保留安全的最小宽度，并在空间不足时干净隐藏内容，不再绘制到下一列。
- 修复压缩包解压目标目录选择器内容居中、层次不齐的问题。目录条目现在采用统一左对齐的树状行，图标和箭头位置固定。
- 修复编辑器缩小窗口后标题、编码/换行/语言控件、版本选项、编辑区和底部按钮相互挤压的问题。现在会根据编辑器自身宽度统一重排，窄窗口下仍保持可操作的编辑区域。
- 统一下拉控件样式，编码、换行符、语言等选择框现在使用相同的分隔线和单个稳定箭头，不再混用合并箭头或重复箭头。
- 修复重复打开同一个 SFTP 文件时不断创建多个编辑器窗口的问题。现在同一连接和远端路径会复用已有编辑器，编辑器最小化后再次打开也会自动恢复。
- 修复切换到其他标签并最小化 Terma 后，原 SFTP 标签目录读取一直停住的问题。后台请求现在会继续完成并写入对应标签，返回该标签时会重新绘制内容。
- 修复上传、下载、删除、复制和拖出已经完成后，旧的“进行中”悬浮任务卡仍然显示的问题。收到完成或失败通知时会立即结束对应任务卡，同时在后台刷新服务器最终状态。
- 修复复杂 SVG 图纸预览中，依赖 class 样式的连接线和基于 symbol 的控件局部缺失的问题。清理后的 SVG 内嵌样式现在会在预览的隔离渲染范围内正常生效，保留源文件的线条、填充和控件溢出显示。
- 修复 SVG 预览窗口跟随图纸缩放而变大或变小的问题。现在外层预览窗口保持独立稳定的大小，缩放只作用于内部可滚动的图纸区域。

#### 本次变更

- 本版用户可感知内容由项目维护者直接提交，未关联已合并 PR 或 Issue。

**完整变更**：[v1.4.9...v1.5.0](https://github.com/zmide/Terma/compare/v1.4.9...v1.5.0)
