# 终端 AI

终端 AI 可以读取当前终端上下文，帮助解释输出、拆分排查步骤、生成命令，并把经过确认的命令发送到终端执行。

## 推荐用法

1. 先让 AI 说明目标、风险和只读检查步骤。
2. 对包含删除、重启、权限变更或批量操作的命令逐条确认。
3. 执行后让 AI 基于实际输出继续判断，不要仅凭计划文本下结论。

聊天模式适合只读问答；执行模式才会生成并发送命令。无论哪种模式，都不要把私钥、Token、密码或完整业务日志直接发送给模型。

## 供应商设置

在设置中配置 AI 供应商、模型、API 地址和推理强度。敏感凭据应使用加密配置，并按最小权限原则选择模型和网络范围。
